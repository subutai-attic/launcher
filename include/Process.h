#ifndef __PROCESS_H__
#define __PROCESS_H__

namespace SubutaiLauncher {
    class Process {
        public:
            void runBasic(std::string command, std::vector<std::string> args);
            void launch(std::string cmd, std::vector<std::string args);
        private:
    };
};

#endif
